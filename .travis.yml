language: python
sudo: false
python:
  - "2.7"

before_install:
  - pip install codecov

install:
  - travis_retry pip install .
  - pip install -r requirements.txt
  - pip freeze

script:
  - make coverage

after_success:
  - codecov
